import styled from '@emotion/styled'

const ResetterWrapper = styled.div({
    boxSizing: 'border-box',
    width: 'max-content',
    height: 'max-content',
    display: 'inline-flex',
    justifyContent: 'center',
    alignItems: 'center',
    padding: '0rem',
    margin: '0rem',
})

export default ResetterWrapper