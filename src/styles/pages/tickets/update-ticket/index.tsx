import styled from "@emotion/styled"

const TicketPagesWrapper = styled.div({
    boxSizing: 'border-box',
    width: '100%',
    maxWidth: '100%',
    minWidth: '100%',
    height: '100%',
    maxHeight: '100%',
    minHeight: '100%',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'center',
    // position: 'relative',
    overflowX: 'hidden'
})

export default TicketPagesWrapper

